# 스윗미 (SwitMe) — 프로젝트 기획서 (PRD)

> **문서 성격**: 현행 상태 문서화 (v2.0)
> **최종 수정**: 2026-04-12

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|------|------|
| **프로젝트명** | 스윗미 (SwitMe) |
| **한 줄 설명** | 유저가 체계적으로 할 일을 계획하고, 회고 데이터를 바탕으로 패턴을 분석하여 개선 방안을 제시하는 앱 |

### 목표 (Goal)

- 유저가 여러 분야(공부, 운동, 업무 등)에서의 계획이 분산되어 혼란해하지 않도록 한다.
- 단순히 계획을 하는 것에 그치지 않고, 회고 데이터 기반 분석과 처방전을 통해 **목표 이행률 상승**과 같은 성과와 발전을 이룰 수 있도록 한다.

### 비전 (Vision)

계획과 관리가 필요한 **모든 사람**이 자신만의 카테고리를 설정하여 계획·실행·분석의 루프를 돌릴 수 있는 범용 생산성 도구.

---

## 2. 타겟 사용자

| 항목 | 내용 |
|------|------|
| **타겟** | 계획과 관리가 필요한 모든 사람 (학생, 직장인, 자기개발 등) |
| **핵심 Pain Point** | 카테고리별 계획이 분산되어 관리가 어렵고, 계획만 세우고 실행·분석까지 이어지지 않음 |

---

## 3. 핵심 기능 정의 (Scope)

### 3-1. MVP 기능

#### 대시보드

| 기능 | 설명 | 구현 상태 |
|------|------|-----------|
| 타이머 / 스톱워치 | 유저가 선택한 카테고리의 활동 시간을 측정 (카운트다운 또는 스톱워치 모드). STOP은 **일시정지**(값 유지·RESUME으로 재개)이며, 세션 기록은 RESET 또는 타이머 완료 시점에 저장(여러 번 멈췄다 재개해도 한 세션으로 합산). 측정 중·멈춤·선택 모드는 앱 재진입 시 복원. | ✅ 구현 완료 |
| 타이머 시간 설정 | 타이머 숫자 클릭 시 모달에서 시·분·초 셀을 각각 탭하여 텍스트 입력. 프리셋 칩(10·15·30분) 제공. "설정" 버튼으로 값만 적용(시작은 별도 START). 마지막 사용값 자동 기억. 스톱워치 모드 클릭 시 안내 토스트. | ✅ 구현 완료 |
| 타임테이블 | 측정한 활동 기록을 24시간 × 10분 단위 그리드에 카테고리 색상으로 표시 | ✅ 구현 완료 |
| 할 일 관리 | 카테고리별 할 일 추가·삭제·완료 토글 | ✅ 구현 완료 |
| 날짜 설정 | 날짜별로 다른 계획을 세울 수 있도록 날짜 선택 기능 제공 | ✅ 구현 완료 |

#### 분석

| 기능 | 설명 | 구현 상태 |
|------|------|-----------|
| 종합 점수 | 회고 데이터 기반 기간별(7일/30일) 평균 점수 산출, 이전 기간 대비 증감 표시 | ✅ 구현 완료 |
| 카테고리별 트렌드 그래프 | 카테고리별 일별 활동 시간(분)을 멀티라인 차트로 시각화 | ✅ 구현 완료 |
| 인사이트 | 트렌드 데이터에서 패턴(요일별 변동, 카테고리 상승/하락, 미활동 카테고리, 회고 기록률)을 자연어 카드로 제시 | ✅ 구현 완료 |
| 처방전 | 종합 점수·최약 카테고리·최약 요일을 종합하여 맞춤 개선 방안 제시 | ✅ 구현 완료 |

> **기존 학습 성향 테스트(7문항 → 페르소나 판별)는 v2.0에서 제거됨.** 유저가 직접 축적한 회고 데이터와 활동 시간을 기반으로 분석한다.
> **카테고리별 트렌드의 지표**: 활동 카테고리(과목 등)별 **일별 활동 시간(분)**. 회고 점수는 항목별로 세분된 카테고리 점수가 없으므로, 활동 카테고리 단위로는 타임테이블 history에서 집계한 활동 시간을 사용한다.

#### 설정

| 기능 | 설명 | 구현 상태 |
|------|------|-----------|
| 카테고리 관리 | 카테고리 추가·삭제, 이름 변경, 색상 설정 (대시보드 할 일·타임테이블 표시에 사용) | ✅ 구현 완료 (UI 상 "과목"으로 표시, 코드 리네이밍 예정) |
| 회고 항목 설정 | 사용자가 회고 평가 항목을 직접 추가·삭제·수정 | ❌ 미구현 |
| 하루 회고 | 목표 달성률(자동 계산) + 사용자 정의 항목(각 수동 입력) = 총점 | ✅ 부분 구현 (현재 고정 항목, 사용자 정의 전환 예정) |
| 습관 (위클리 + 매일 반복) | 요일별·매일 반복할 할 일과 플랜을 등록. 우선순위: 매일 < 요일 < 사용자 수정. **시드 트리거**: ① 그날 대시보드 첫 진입 (lazy) ② 습관 편집 직후 ③ 새 플랜 탭 생성 시 ④ 기록 탭 → 플랜 탭 전환 시. 시드 대상은 **모든 비-습관 플랜 탭**이며 시드 로그는 탭별로 분리되어 재시드를 차단한다. | ✅ 구현 완료 |

#### 캘린더

| 기능 | 설명 | 구현 상태 |
|------|------|-----------|
| 월별 캘린더 | 회고 점수와 오늘 날짜 표시, 월 단위 네비게이션 | ✅ 구현 완료 |

### 3-2. 후순위 기능

- 구글 캘린더와 프로그램 캘린더 연동

### 3-3. 명시적 제외 항목 (Out of Scope)

- 학습 성향 테스트 (v2.0에서 제거)

---

## 4. 기술 스택 및 아키텍처

### 기술 스택

| 영역 | 기술 |
|------|------|
| **프론트엔드** | HTML5, Vanilla CSS, Vanilla JavaScript (ES6+ Modules) |
| **디자인 시스템** | 다크 모드, 글래스모피즘(Glassmorphism), Inter + Outfit 폰트 |
| **아이콘** | Lucide Icons (CDN) |
| **차트** | Chart.js (CDN) |
| **상태 관리** | `localStorage` 기반 클라이언트 사이드 저장 |
| **빌드** | Vite |
| **PWA** | `vite-plugin-pwa` (Service Worker 자동 생성, 오프라인 지원) |
| **코드 품질** | ESLint (Flat Config) + Prettier |
| **테스트** | Vitest + jsdom |
| **배포** | Vercel (GitHub 푸시 → 자동 빌드·배포) |

### 아키텍처

```
브라우저 (클라이언트 전용)
├── localStorage ←→ store.js (전역 상태 관리)
│                      ↕
│          ┌───────────┼───────────┐
│          ↓           ↓           ↓
│      timer.js    tasks.js    analysis.js
│      (세션 기록)  (태스크 CRUD) (분석 결과)
│          ↓           ↓
│      timetable.js   ui.js ←→ mobile.js
│      (시각화)       (데스크탑)   (모바일)
│
├── 서버 없음 — 모든 데이터는 브라우저 로컬에 저장
└── PWA로 오프라인 사용 및 앱 설치 지원
```

### 데이터 저장 (localStorage 키)

| 키 | 내용 |
|----|------|
| `switme_tasks` | 할 일 목록 |
| `switme_history` | 활동 세션 기록 |
| `switme_subjects` | 카테고리 목록 |
| `switme_reflection_items` | 사용자 정의 회고 항목 목록 |
| `switme_reflections` | 날짜별 하루 회고 |
| `switme_analysis` | 분석 결과 이력 |
| `switme_habits` | 요일별·매일 반복 습관 정의(할 일) |
| `switme_habit_seed_log` | 날짜별 시드 기록. `{ taskKeys: [], planKeysByTab: { [tabId]: [] } }` 구조로 **할 일은 단일 리스트, 플랜은 탭별 분리**. 한 번 시드된 항목은 같은 날 재시드되지 않는다. |
| `switme_timetables` | 타임테이블 목록 (비-습관 사용자 탭은 `type: 'record' \| 'plan'`로 종류 결정, 기록·플랜 각 최소 1개 보장 / `isHabit: true` 습관 8개 — 요일 7 + 매일 1) |
| `switme_active_timetable_id` | 현재 활성 타임테이블 ID |
| `switme_timer` | 타이머/스톱워치 상태 — 측정 중·멈춤(일시정지)·선택 모드 (앱 재시작 복원용) |
| `switme_timer_last_duration` | 타이머 마지막 설정 시간(초). 다음 모달 진입 시 초기값으로 사용. |

---

## 5. 화면 구조

PC(데스크탑)와 모바일 두 가지 셸(Shell)로 분리된 레이아웃. 동일한 state를 공유하며, 디바이스에 따라 자동 전환된다.

### 5-1. PC (Desktop Shell)

```
┌─────────┬──────────────────────────────────────┐
│ Side    │ Top Bar (제목 / 날짜 / 몰입모드)       │
│ Nav     ├────────────┬──────────┬───────────────┤
│         │ 할 일 목록  │ 타이머   │ 타임테이블     │
│ (68px)  │ + 하루 회고 │ (히어로) │ (24h 그리드)  │
│         ├────────────┴──────────┴───────────────┤
│ 탭:     │ [캘린더] [분석] [설정] — 탭 전환으로 표시 │
│ 대시보드 │                                        │
│ 캘린더   │                                        │
│ 분석     │                                        │
│ 설정     │                                        │
└─────────┴──────────────────────────────────────┘
```

### 5-2. 모바일 (Mobile Shell)

```
┌──────────────────────────────────┐
│ 헤더 (날짜 / 몰입 버튼)             │
├──────────────────────────────────┤
│ 타이머 히어로 (SVG 링 + START)     │
│ 탭 네비게이션 (4버튼) + RESET       │
├──────────────────────────────────┤
│ 콘텐츠 영역 (대시보드 기준)          │
│ ┌──────────────┬───────────────┐ │
│ │ [할일][회고]  │ 타임테이블      │ │  ← 좌·우 가로 분할
│ │  ▔▔▔         │ ─────────────  │ │
│ │  (컬럼 탭)    │ [기록][플랜1][+]│ │  ← 탭이 곧 종류 (기록/플랜)
│ │              │ ─────────────  │ │
│ │  할 일 목록   │ 24h × 10분 그리드│ │
│ │   또는       │                │ │
│ │  회고 입력    │                │ │
│ └──────────────┴───────────────┘ │
│ (캘린더/분석/설정 패널은 탭으로 전환) │
└──────────────────────────────────┘
```

> **모바일 레이아웃 규칙 (할 일 + 타임테이블이 함께 노출되는 모든 화면에 적용)**
> - 좌측 컬럼: `[할 일] [회고]` 컬럼 탭으로 전환 (`m-col-tabs`).
> - 우측 컬럼: 타임테이블 자체 탭바(기록 탭 + 플랜 탭, 각 종류 최소 1개 보장) + 24h 그리드.
> - 좌·우는 **가로 분할**을 유지한다 (모바일이라고 세로 적층/컬럼 탭 단일화로 바꾸지 않는다).
> - 신규 화면(예: 설정 > 습관)도 이 패턴을 따른다. 단, 화면 목적에 따라 좌측의 회고 탭은 숨길 수 있다.

### 5-3. 공유 컴포넌트

| 컴포넌트 | 설명 |
|----------|------|
| Zen Mode Overlay | 전체화면 몰입 모드 (카테고리명 + 대형 타이머) |
| Task Modal | 할 일 추가 모달 (카테고리 선택 + 내용 입력) |

---

## 6. 성공 기준 (Definition of Done)

> *기획서 초안 작성 후 논의 예정*

---

## 7. 일정 및 마일스톤

> *기획서 초안 작성 후 논의 예정*

---

## 8. 리스크 및 제약사항

| 항목 | 설명 |
|------|------|
| 웹 기반 한계 | 백그라운드 상시 실행, 센서·하드웨어 접근 등 일부 네이티브 기능 사용 불가. **푸시 알림은 사용 가능** (안드로이드 Chrome은 지원, iOS는 Safari 16.4+ **설치형 PWA 한정** 지원) |
| 데이터 영속성 | `localStorage` 기반이므로 브라우저 데이터 삭제 시 모든 데이터 유실 |
| 서버 미사용 | 기기 간 데이터 동기화 불가, 백업 기능 없음 |

> **정정 이력 (2026-07-15):** 기존 "푸시 알림 등 네이티브 기능 사용 불가" 기술은 사실이 아니었다. Web Push는 안드로이드에서 오래전부터, iOS에서도 16.4(2023)부터 설치형 PWA에 한해 지원된다. 이 오기재로 인해 **알림 기반 리텐션 설계가 검토 대상에서 제외되어 있었다.** 향후 리텐션 기능 기획 시 푸시는 현 스택으로 구현 가능한 선택지로 취급한다.

---

## 9. 수익 모델 ☐ 미착수 (스택 확정 2026-07-19)

> **상태:** 검토 완료. **현 구조(서버·계정 없음)에서는 유료화 자체가 불가능**하므로 로그인·백엔드 도입이 선행 조건이었다. Phase 3에서 백엔드(Firebase)·인앱결제(Google Play IAP) 도입이 **확정**되어 결제 구현이 기술적으로 가능해졌다 — 결정 근거는 §9-5.
>
> **⚠️ 목적 유의 (2026-07-21 개정):** 결제를 붙이되 목적은 **학습(앱 개발 전 과정 경험)**이다. 다만 **샌드박스에서 실결제로 전환**되었다 — 정산·수수료·세금·환불이 학습 대상에 포함되기 때문. 결정은 §9-6. 성공 기준은 매출액이 아니라 **각 과정의 완주 여부**이므로 §10-5 뒤집기조건 #1은 여전히 미발동(§10-2 판정 참조).

### 9-1. 현 구조의 제약

| 제약 | 수익화에 미치는 영향 |
|---|---|
| 서버 없음, `localStorage` 단독 | 결제 검증·구독 상태 확인 불가 |
| 기기 간 동기화 불가 | 유저가 지불할 가장 명확한 이유(데이터 안전·기기 이동)가 부재 |
| 도메인별 `localStorage` 분리 | 배포 주소가 다르면 데이터도 분리 (§개선사항목록 기록 참조) |

### 9-2. 채택 후보 — Freemium

무료 기능은 유지하고, **데이터가 축적될수록 가치가 커지는 것**만 유료로 잠근다.

| 구분 | 범위 |
|---|---|
| **무료** | 타이머·타임테이블·할 일·습관·캘린더·회고 기록 전부 + 단기(7일) 분석 |
| **유료 후보 1** | 계정 동기화·백업 — 기기 간 이동, 브라우저 데이터 삭제 시 복구 |
| **유료 후보 2** | 장기 분석 — 30일/90일/연간 트렌드, 기간 비교 |
| **유료 후보 3** | 처방전 심화 — 규칙 기반이 아닌 개인 이력 축적 기반 |
| **유료 후보 4** | 데이터 내보내기 — CSV·이미지 리포트 |

**근거:** 타이머·할 일은 대체재가 많아 유료화 시 즉시 이탈한다. 반면 유저 본인의 축적 데이터는 대체 불가이며, 스윗미의 차별점(회고→분석→처방 루프)도 시간 축적이 전제라 유료 라인과 겹친다.

### 9-3. 명시적 제외 — 검토 후 기각

| 기각안 | 사유 |
|---|---|
| 광고 | 몰입 도구라는 정체성 훼손. 유의미한 수익엔 대규모 트래픽 필요 |
| 선불 구매(앱 유료 판매) | 웹 결제 선불은 전환율이 낮음 |
| 일회성 라이선스 | 서버 유지비는 계속 발생하는데 수입은 1회 → 구조적 적자 |

### 9-4. 참고 — 스토어 결제 수수료

앱 내에서 디지털 상품을 판매할 경우 스토어 자체 결제가 강제된다 (Google Play 기준 수수료 15%, 연매출 100만 달러 미만). 앱 내부에서 자체 웹 결제창을 띄우는 것은 정책 위반이다. 웹(브라우저)에서만 판매할 경우엔 해당 없음.

> **TWA 앱 내 결제 가능 여부 (2026-07-19 확인):** TWA 앱 안에서도 인앱결제가 **된다**. `Digital Goods API` + `Payment Request API`로 Google Play Billing을 호출하는 방식이며 Google 공식 지원(Chrome 101+, 샘플 `pwa-play-billing`). 금지되는 것은 Stripe 등 **자체 웹 결제창**이고, Play 공식 IAP는 오히려 디지털 상품 판매 시 강제되는 표준이다. 출처: Chrome for Developers — Receive Payments via Play Billing.

### 9-5. 백엔드·결제 스택 결정 (2026-07-19)

> 5부 기록 형식. 근거는 여기, 실행 체크리스트는 `docs/phase3-할일.md`(중복 금지, 참조만).

**결정:** Phase 3 백엔드를 **Firebase(BaaS)**로, 결제를 **Google Play 인앱결제(IAP)**로 확정한다. 인증·데이터 동기화·푸시 알림·결제 검증을 모두 Firebase에서 처리한다.

**⚠️ 전제:** 현재 목표는 **학습**이며, 학습 범위에 로그인·결제 흐름이 포함된다(§10-2). 매출 극대화가 아니라 과정 경험이 목적. 이 전제가 무너지면(성공 기준이 유저 수·매출액으로 바뀌면) 스택 요구가 재산정된다.

> **2026-07-21 갱신:** 본 스택 결정 자체는 유효하다. 결제가 샌드박스→실결제로 바뀌었으나 Firebase·IAP 조합은 그대로이며, 오히려 실결제가 IAP 선택을 더 강하게 지지한다. 변경 내역은 §9-6.

**기준별 근거:**

| 기준 | 근거 |
|---|---|
| 배움의 밀도 (학습 1순위) | TWA·IAP·푸시·구글 로그인이 **전부 구글 생태계**라 인증→동기화→결제→푸시 흐름이 한곳에서 맞물림 → 학습 흐름 안 끊김 |
| 개발 기간 = 산출물 (학습 목표) | Firebase는 인증·DB·동기화를 즉시 제공 → 일 2시간 가용으로 몇 주에 a~z 경험. 직접 서버 구축은 같은 학습을 수개월 더 비싸게 삼 |
| 되돌림 비용 | Firebase → 타 백엔드 이전은 데이터 이사 필요하나 학습 규모라 작음 → **양방향 문** |
| 결제 위치 (진짜 병목) | IAP는 TWA 학습과 일관. TWA 앱 내 IAP는 기술적으로 가능(§9-4 확인) |

**기각한 대안:**

| 대안 | 사유 |
|---|---|
| Supabase | 표준 SQL 학습 전이성은 우위이나, 결제(구글)·푸시와 **이종 조합**이라 학습 흐름 마찰. 목표가 "DB 이론"이 아니라 "시장 운영 경험"이라 SQL 우위의 가치가 낮음 |
| 직접 서버 구축(Node+DB) | 백엔드 심화 학습엔 좋으나 일 2시간이면 수개월 — 학습 목표(시장 a~z) 대비 과투자 |
| 웹 결제(Stripe) | TWA 앱 안에서 못 굴림(브라우저 전용). TWA 출시 학습과 단절 |

**⚠️ 뒤집어야 하는 조건:**

1. 목표가 실제로 "학습 → 제품(수익·유저)"으로 전환됨 → 확장성·비용 기준이 새로 지배, 스택 재검토(§10-5 #1과 연동).
2. iOS 지원이 필요해짐 → IAP(구글)·TWA 전제 붕괴.
3. Firebase 무료 티어 한도를 학습 트래픽이 초과 → 비용 구조 재검토.

**알려진 미해결 리스크:**

| 리스크 | 내용 | 해소 조건 |
|---|---|---|
| 벤더 종속 | Firebase 특정 API에 코드가 묶임 | 학습 규모라 이전 비용 작음(양방향 문). 감내 |
| NoSQL 모델링 | Firestore는 관계형이 아니라 데이터 설계가 다름 | Phase 3 동기화 설계 시 학습 대상으로 흡수 |
| ~~실결제 미경험~~ | ~~샌드박스만 쓰면 실제 정산·환불·세금 흐름은 못 겪음~~ | **✅ 해소 (2026-07-21).** 실결제 전환으로 소멸 → §9-6 |

### 9-6. 실결제 전환 결정 (2026-07-21)

> 5부 기록 형식. §9-5의 "샌드박스·실수익 없음"을 **대체**한다. 실행 체크리스트는 `docs/twa-출시-할일.md`·`docs/phase3-할일.md`(중복 금지, 참조만).

**결정:** 결제를 **샌드박스에서 실결제로 전환**한다. 유료 상품 **1개·최저가**, 홍보는 **자체 제작 콘텐츠 중심의 소액**. 공개 출시까지 진행한다.

**⚠️ 전제 (목표 정의 확장):**

> 앱 개발의 전 과정을 직접 겪는다. 기획·구현·출시뿐 아니라 **유지보수·홍보·매출·정산·세금·환불 대응까지 포함**한다. **성공 기준은 유저 수나 매출액이 아니라 각 과정을 완주했는가**이다.

이 전제가 무너지면(성공 기준이 숫자로 바뀌면) §10-5 #1이 발동하고 본 결정과 §10 전체를 재검토한다.

**기준별 근거:**

| 기준 | 근거 |
|---|---|
| 목표가 "전 과정 경험" | 샌드박스는 정의상 **돈·법·세금이 빠진** 버전이라 목표 미달. 기술 흐름만 같음 |
| 공개 출시 필요성 | 낯선 유저 반응·공개 별점 관리는 **공개 트랙만** 제공. 비공개로 대체 불가 |
| 규모 완화 (원칙 8 — 2단계) | 회피는 목표 미달이므로 불가. **상품 1개·최저가·소액 홍보**로 학습 손실 없이 금액 규모만 최소화 |
| 결제 위치 | 실결제도 IAP로 동일 처리. §9-5 스택 결정 불변 |

**기각한 대안:**

| 대안 | 사유 |
|---|---|
| 샌드박스 유지 | 정산·수수료·세금·환불을 못 겪음 → 목표 미달 |
| 비공개 테스트 종료 | 유저 반응·별점 관리 경험 상실 → 목표 미달 |
| 규모 제한 없는 홍보 | 학습 산출물은 동일한데 세무 부담만 증가 |

**⚠️ 뒤집어야 하는 조건:**

1. 성공 기준이 "과정 완주" → "유저 수·매출액"으로 이동 → §10-5 #1 발동, §10 전체 재검토.
2. 사업자등록·통신판매업 확인 결과 부담이 감당 범위를 넘음 → 샌드박스 복귀 검토.
3. 매출 규모가 예상을 크게 초과 → 세무 구조 재산정.

**알려진 미해결 리스크:**

| 리스크 | 내용 | 해소 조건 |
|---|---|---|
| **세무·법적 의무** | 사업자등록 시 부가세(1·7월)·종합소득세(5월) **정기 신고 의무**가 발생하며 **폐업 신고 전까지 지속**. 매출 0원이어도 무실적 신고 필요 | 없음(구조적). **⚠️ 필요 여부는 국세청 126·관할 구청에 직접 확인 — 본 문서 서술을 근거로 진행 금지** |
| 통신판매업 신고 | 온라인 판매 시 지자체 신고. 사업자등록과 **별개 절차**. 소규모 면제 규정 존재 여부 미확인 | 위와 동일 — 직접 확인 |
| 공개 별점 | 공개 트랙 진입 시 별점·리뷰가 **영구 기록**(일방향 문) | 없음. 목표상 감내 |
| 환불 대응 | 자동 환불 기간 이후 요청은 개발자가 판단·응대. 공개 연락처 필요 | 방침 문서화로 완화 → 할 일 문서 항목 |

**정정 이력 (5부 — 조용히 고치지 않는다):**

- 최초 트리거 #1 미발동 근거로 **"유저 확보 동기 없음 — 홍보 행동이 추가되지 않았다"**를 제시했으나, 홍보가 학습 범위에 포함되면서 **사실과 달라져 철회**했다. 대체 근거는 "성공 기준이 숫자가 아니라 완주 여부"다.

---

## 10. 플랫폼 전략 — TWA 결정 (2026-07-15)

### 10-1. 결정

**TWA(Trusted Web Activity)로 Google Play 진입. 네이티브 앱 개발은 보류.**

TWA는 기존 PWA를 안드로이드 앱으로 포장하는 방식이다. 코드 재작성이 없고, 웹 배포 시 앱에도 즉시 반영된다(스토어 재심사 불필요).

### 10-2. 결정의 전제 — ⚠️ 이것이 바뀌면 결정도 무효

> **현재 목표는 "학습"이다 (2026-07-21 최종 정의):**
>
> **앱 개발의 전 과정을 직접 겪는다.** 기획·구현·출시뿐 아니라 **유지보수·홍보·매출·정산·세금·환불 대응까지 포함**한다. **성공 기준은 유저 수나 매출액이 아니라 각 과정을 완주했는가**이다.

> **학습 범위 확장 이력**
>
> - **2026-07-19 (1차):** 로그인·계정 동기화·인앱결제(IAP) 연동 흐름을 학습 대상에 포함(Phase 3). 이 시점엔 **결제를 샌드박스로 한정**했다.
> - **2026-07-21 (2차):** 샌드박스는 **돈·법·세금이 빠진 버전**이라 "전 과정 경험"에 미달함이 드러났다. **실결제·공개 출시·홍보**를 범위에 포함한다. 결정은 §9-6.

> **§10-5 뒤집기조건 #1 재판정 (2026-07-21): 미발동.**
>
> | # | 근거 |
> |---|---|
> | 1 | **수익이 목적이 아니라 학습 소재다.** 실결제를 켜는 이유는 정산·수수료·세금·환불이 어떻게 도는지 겪기 위함이며, 매출은 부산물이다 |
> | 2 | **성공 기준이 숫자가 아니다.** 홍보를 하더라도 판정 기준은 "홍보→유입→매출→환불→세금 신고를 한 바퀴 겪었는가"이지 "몇 명 모았는가"가 아니다. 제품 목표라면 숫자가 기준이 된다 |
> | 3 | **트리거가 격상시킬 리스크가 이미 해소 예정이다.** #1의 실효는 "데이터 유실이 감내 불가로 격상되고 서버가 필수가 된다"인데, Phase 3에서 서버 도입이 이미 확정이다 |
>
> **철회된 근거:** 초안에서 근거 ②로 "유저 확보 동기 없음 — 홍보 행동이 추가되지 않았다"를 썼으나, 홍보가 학습 범위에 포함되면서 사실과 어긋나 **명시적으로 철회**했다(원칙 §4-3). 위 표 2번이 대체 근거다.

### 10-3. 판단 기준별 근거

| 기준 | 값 | TWA를 지지하는가 |
|---|---|---|
| 목표 | 학습(스토어 운영) | ✅ TWA가 그 학습을 수일 내 제공. 네이티브는 동일 학습을 수개월 뒤 제공 |
| iOS 필요성 | 불필요 | ✅ 네이티브의 최대 이점(iOS 진입)이 무력화 |
| 가용 시간 | 일 2시간 | ✅ TWA는 수용 가능. 네이티브 재작성은 이 속도로 수개월 |
| 실패 감내 | 별점 사고 감내 가능 | ✅ TWA의 유일한 고유 리스크가 감내 범위 내 |
| 지연 비용 | 학습 시작이 늦어짐 | ✅ TWA는 즉시 착수 가능 |

### 10-4. 기각한 대안

| 대안 | 사유 |
|---|---|
| 네이티브 (Kotlin/Swift 또는 Flutter/RN) | 전면 재작성 필요(현 코드 재활용 ≈ 0). 현 목표가 "앱 개발 학습"이 아니라 "스토어 운영 학습"이므로 동일 학습을 수개월 더 비싸게 사는 셈. iOS 불필요로 최대 이점도 상실. Apple 개발자 계정 연 $99 상시 발생 |
| 현상 유지 (웹만) | 스토어 진입 불가 → 목표(학습) 달성 자체가 불가능 |

### 10-5. 결정을 뒤집어야 하는 조건 (Reversal Triggers)

아래 중 하나라도 발생하면 **§10 전체를 재검토**한다. 결론만 승계하지 말 것.

1. **목표가 "학습" → "제품(유저 확보·수익)"으로 변경.** 이 경우 §9의 선행 조건(로그인·서버)이 되살아나며, 데이터 유실 리스크(§10-6)가 감내 불가 수준으로 격상된다.
2. **iOS 지원이 필요해짐.** TWA는 iOS를 커버하지 못한다 (Apple 미허용).
3. **백그라운드 상시 실행·센서 접근 등 웹이 못 하는 기능이 핵심 요구가 됨.**

### 10-6. 알려진 미해결 리스크

| 리스크 | 내용 | 해소 조건 |
|---|---|---|
| **데이터 유실** | TWA는 Chrome 저장소를 공유한다. 유저가 Chrome에서 "인터넷 사용 기록 삭제"를 실행하면 앱을 삭제하지 않았는데도 기록이 전부 소실된다. 웹에서는 납득되던 동작이 **앱에서는 버그로 인식**된다 | **서버 동기화 도입 시 소멸.** TWA 고유 문제가 아니라 서버 부재로 인한 문제 |
| Chrome 의존 | 기기에 Chrome(또는 호환 브라우저)이 없거나 비활성화되면 주소창이 노출되거나 실행 불가 | 없음 (구조적) |
| 심사 거절 | Google은 TWA를 공식 허용하나, "웹사이트 단순 포장" 앱은 스팸 정책으로 거절한다. 경계는 심사자 재량 | 없음 (기능 충실도로 완화) |
| 연 1회 강제 재빌드 | Google의 Android 타겟 API 레벨 요구치가 매년 상승. 앱을 수정하지 않아도 매년 재빌드·재제출해야 하며, 미이행 시 검색에서 제외 | 없음 (상시 운영 비용) |

### 10-7. 비공개 테스트 — 리스크 완화가 아니라 의무 관문 (2026-07-17 정정)

> **당초 §10-7은 비공개 테스트를 "고를 수 있는 리스크 완화 수단"으로 기술했으나, 이는 부정확했다.** 유저 계정은 **개인 계정**이므로 구글 정책상 비공개 테스트가 **선택이 아니라 강제 경로**다.

- **개인 개발자 계정(2023-11-13 이후 생성)은 공개 출시 권한을 처음부터 받지 못한다.** 비공개 테스트에서 **테스터 12명 이상 × 14일 연속**을 충족하고 프로덕션 권한을 신청(심사 최대 7일)해야 공개가 열린다.
- 따라서 §10-8(원칙 8, 리스크 회피)에서 "별점 리스크를 비공개 테스트로 회피한다"는 판단은 **적용 여지가 없었다** — 회피할지 말지 고를 수 없고 비공개가 의무다. 결과적으로 별점 리스크는 자동 회피되나, 그건 선택의 결과가 아니다.
- **~~남은 유저 판단~~ → 해소 (2026-07-21): 공개 출시까지 간다.** 목표 정의(§10-2)에 홍보·매출·유저 반응·별점 관리가 포함되므로 비공개 종료는 목표 미달이다. 따라서 **12명×14일 관문은 선택이 아니라 필수 경로**로 확정됐다. 결정 근거 §9-6, 실행 `docs/twa-출시-할일.md`.
- **결제 학습에 관한 사실 정정:** 인앱결제 **기술 흐름**(상품 등록·결제창·구매 검증·기능 해제)은 라이선스 테스터를 쓰면 **비공개 테스트에서도 완주 가능**하다. 즉 "결제를 배우려면 공개가 필요하다"는 연결은 성립하지 않는다. 공개 출시를 요구하는 것은 결제가 아니라 **실제 정산·세금·낯선 유저 반응·공개 별점**이다.
- **정책 출처:** Play Console Help — App testing requirements for new personal developer accounts (2024-12-11 개정: 테스터 최소 20→12명).
